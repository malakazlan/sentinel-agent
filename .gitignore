.venv/
__pycache__/
*.pyc
.env
.phoenix/
.DS_Store
phoenix-server.log
*.log

# Reproduction run artifacts (regeneratable via scripts/repro_cold_vs_warm.py)
scripts/_repro.log

# Ad-hoc headless verification runners (one-shot scripts; nothing should depend on them)
_run_*.py
_run_*.out
_run_*.stderr

# Development-only docs — never committed, never pushed
CLAUDE.md
.claude/
context/
docs/superpowers/
design/

# Next.js frontend
web/node_modules/
web/.next/
web/out/
web/.env.local
web/next-env.d.ts
web/*.tsbuildinfo

# Playwright
test-results/
playwright-report/
web/tests-e2e/.auth/
node_modules/

# Phase 7 / ADR-013 — local incident memory store (machine-local; not public)
# Demo seeds (prompt_history.jsonl, incidents.jsonl) are an exception —
# they ship in the Docker image so the live URL renders populated pages.
data/memory/*
!data/memory/prompt_history.jsonl
!data/memory/incidents.jsonl
